# Todas as variáveis têm default sensato — o .env é opcional.
# A API do Open-Meteo é pública e não precisa de chave.

# Anos a extrair: intervalo ("2020-2025") ou lista ("2024,2025")
ANOS=2020-2025

# Onde ficam o Parquet cru e o arquivo do DuckDB
DATA_DIR=./data
DUCKDB_PATH=./data/clima.duckdb
